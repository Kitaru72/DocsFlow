# DocsFlow

DocsFlow - консольное Python-приложение для подготовки данных лабораторного отчета и генерации DOCX-документа.

Проект начался как учебная автоматизация оформления лабораторных работ, но постепенно развивается в полноценный инструмент для работы с профилями, данными отчетов, JSON-хранилищем, валидацией данных, ООП-моделями, тестами и генерацией документов.

## Текущий статус

Сейчас реализована CLI-версия приложения с меню. Пользователь может создавать и загружать профили, создавать и загружать данные отчета, а затем генерировать DOCX-отчет на основе выбранных данных.

В проект также добавлены базовые тесты, ООП-модели профиля и данных отчета, а также CI через GitHub Actions.

## Возможности

- CLI-меню для выбора действий.
- Создание профиля через консоль.
- Загрузка сохраненного профиля из списка.
- Создание данных лабораторного отчета:
  - дисциплина;
  - номер лабораторной работы;
  - тема;
  - список заданий.
- Загрузка сохраненных данных отчета из списка.
- Нормализация строковых данных: удаление лишних пробелов.
- Валидация обязательных полей.
- Проверка структуры загруженных данных.
- ООП-модели `Profile` и `ReportData`.
- Безопасный ввод положительного числа для количества заданий.
- Форматированный вывод профиля и данных отчета в консоль.
- Сохранение профилей в JSON.
- Сохранение данных отчетов в JSON.
- Поиск JSON-файлов в директориях профилей и отчетов.
- Обработка отсутствующих и некорректных JSON-файлов.
- Автоматическое создание директорий при сохранении файлов.
- Очистка имен файлов от запрещенных символов.
- Объединение профиля и данных отчета в структуру для генерации документа.
- Генерация DOCX-отчета через `python-docx`.
- Уникальное имя DOCX-файла на основе профиля и номера лабораторной работы.
- Базовые тесты на `pytest`.
- Автоматический запуск тестов в GitHub Actions.

## Структура проекта

```text
DocsFlow/
├── app/
│   ├── __init__.py
│   ├── cli.py
│   ├── docx_generator.py
│   ├── models/
│   │   ├── __init__.py
│   │   ├── profile.py
│   │   └── report.py
│   ├── profiles.py
│   ├── reports.py
│   ├── storage.py
│   └── validation.py
├── data/
│   ├── profiles/
│   ├── reports/
│   └── .gitkeep
├── output/
│   └── .gitkeep
├── tests/
│   ├── test_profile_model.py
│   ├── test_profiles.py
│   ├── test_report_model.py
│   ├── test_reports.py
│   ├── test_storage.py
│   └── test_validation.py
├── .github/
│   └── workflows/
│       └── ci.yml
├── main.py
├── pyproject.toml
├── uv.lock
├── .python-version
├── .gitignore
└── README.md
```

## Назначение модулей

`main.py` - точка входа в приложение. Запускает CLI-интерфейс.

`app/cli.py` - консольный интерфейс и пользовательские сценарии: создание профиля, загрузка данных, выбор файлов, генерация DOCX.

`app/profiles.py` - логика профилей: создание, нормализация и форматирование.

`app/reports.py` - логика данных отчета: создание, нормализация и форматирование.

`app/storage.py` - работа с JSON-файлами, путями, поиском сохраненных данных и безопасными именами файлов.

`app/validation.py` - проверка структуры профилей и данных отчетов.

`app/docx_generator.py` - объединение данных и генерация DOCX-документа.

`app/models/profile.py` - модель профиля пользователя.

`app/models/report.py` - модель данных лабораторного отчета.

## Установка и запуск

Проект использует `uv` для управления окружением и зависимостями.

Установить зависимости:

```powershell
uv sync
```

Запустить приложение:

```powershell
uv run python main.py
```

## CLI-меню

```text
1. Создать профиль
2. Загрузить профиль
3. Создать данные отчета
4. Загрузить данные отчета
5. Сгенерировать DOCX-отчет
0. Выйти
```

## Тесты

Проект использует `pytest`.

Запустить все тесты:

```powershell
uv run pytest
```

Сейчас тестами покрыты:

- модели `Profile` и `ReportData`;
- функции создания профилей и данных отчетов;
- очистка имен файлов от запрещенных символов;
- проверка структуры профиля;
- проверка структуры данных отчета;
- отклонение пустых полей;
- отклонение неверных типов данных;
- отклонение некорректного списка заданий;
- принятие корректных профилей и данных отчетов.

## Где хранятся данные

Профили сохраняются в:

```text
data/profiles/
```

Данные отчетов сохраняются в:

```text
data/reports/
```

Сгенерированные DOCX-файлы сохраняются в:

```text
output/
```

Сгенерированные JSON и DOCX-файлы не добавляются в Git, потому что это пользовательские данные и результаты работы программы.

## Используемые технологии

- Python
- uv
- pytest
- GitHub Actions
- JSON
- pathlib
- python-docx
- Git и GitHub

## Что отработано в проекте

- базовая структура Python-проекта;
- разделение ответственности между модулями;
- функции и возвращаемые значения;
- словари и списки;
- работа со строками;
- обработка ошибок через `try` / `except`;
- чтение и запись JSON;
- работа с путями через `pathlib`;
- безопасный пользовательский ввод;
- консольное меню;
- базовые ООП-модели;
- валидация загруженных данных;
- генерация DOCX-документов;
- базовые unit-тесты;
- CI для автоматического запуска тестов на GitHub;
- управление зависимостями через `uv`;
- регулярная работа с Git: `status`, `diff`, `add`, `commit`, `push`.

## План развития

- Постепенно встроить ООП-модели глубже в проект.
- Расширить набор тестов для JSON-хранилища и генерации DOCX.
- Улучшить форматирование DOCX-документа.
- Добавить шаблоны отчетов.
- Продумать следующий крупный шаг после ООП-рефакторинга: PostgreSQL.
- Позже вынести приложение в FastAPI API.
